---
const sponsors = [
  "icon-[custom--isd]",
  "icon-[custom--isd]",
  "icon-[custom--isd]",
  "icon-[custom--isd]",
  "icon-[custom--isd]",
  "icon-[custom--isd]",
];
---

<div class="flex flex-col items-center justify-center gap-2 text-center">
  <p class="md:ml-auto">Sponsored by</p>
  <div class="grid grid-cols-3 gap-3 text-black">
    {
      sponsors.map((sponsor) => (
        <div class="relative flex h-16 w-16 items-center justify-center text-3xl outline-dashed outline-2 outline-white">
          <i class={sponsor} />
        </div>
      ))
    }
  </div>
</div>
